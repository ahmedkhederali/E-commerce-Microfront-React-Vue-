name: deploy-container

on:
  push:
    branches:
      - main
    paths:
      - 'packages/container/**'

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://ecommerce-mfe-dashboard2/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIAWSURU6TB32QP4UE7
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: eu-north-1
